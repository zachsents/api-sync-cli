name: Release

on:
  push:
    branches: [main]
    paths:
      - package.json

permissions:
  contents: write

jobs:
  release:
    name: Publish to npm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for version bump
        id: diff
        run: |
          OLD=$(git show HEAD~1:package.json 2>/dev/null | grep '"version"' | head -1 | sed 's/.*: "//;s/".*//')
          NEW=$(grep '"version"' package.json | head -1 | sed 's/.*: "//;s/".*//')
          if [ "$OLD" = "$NEW" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "version=$NEW" >> "$GITHUB_OUTPUT"
          fi

      - uses: oven-sh/setup-bun@v2
        if: steps.diff.outputs.changed == 'true'

      - run: bun install
        if: steps.diff.outputs.changed == 'true'

      - name: Check
        if: steps.diff.outputs.changed == 'true'
        run: bun run check

      - name: Build
        if: steps.diff.outputs.changed == 'true'
        run: bun run build

      - name: Publish to npm
        if: steps.diff.outputs.changed == 'true'
        run: bun publish --access public
        env:
          NPM_CONFIG_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub release
        if: steps.diff.outputs.changed == 'true'
        run: |
          gh release create "api-sync-cli@${{ steps.diff.outputs.version }}" \
            --title "api-sync-cli@${{ steps.diff.outputs.version }}" \
            --generate-notes \
            --target "${{ github.sha }}"
        env:
          GH_TOKEN: ${{ github.token }}
